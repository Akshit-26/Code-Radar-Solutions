#include <stdio.h>
#include <string.h>

int main(){
    char str[];
    int n = strlen(str);
    int count = 0;
    for(int i=0; i<n; i++){
        if(str[i]>=a && str[i]<=z){
            count+=1;
        }
    }
    return 0;
}